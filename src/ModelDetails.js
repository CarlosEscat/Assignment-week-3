import * as React from 'react'


export default class ModelDetails extends React.Component {
  render () {
    
    //const model = this.props.state
    return (`the computer model details go here`
    // <div>
    //   <ul>
    //     <li key={model.name}>{ model.name }</li>
    //     <li key={model.manufacturer}>{ model.manufacturer }</li>
    //     <li key={model.year}>{ model.year }</li>
    //     <li key={model.origin}>{ model.origin }</li>
        
    //   </ul>
    // </div>
    )
     
  }
}

//`the computer model details go here`
//${this.props.match.params.name}